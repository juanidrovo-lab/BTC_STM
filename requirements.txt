# ── ASGI server (replaces Vercel/Mangum runtime) ─────────────────────────────
uvicorn[standard]>=0.29,<1

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.110,<1
python-multipart>=0.0.9,<1

# ── HTTP client (Binance REST) ────────────────────────────────────────────────
httpx>=0.27,<1

# ── Settings and validation ───────────────────────────────────────────────────
pydantic>=2.7,<3
pydantic-settings>=2.3,<3

# ── Database ──────────────────────────────────────────────────────────────────
asyncpg>=0.29,<1
sqlalchemy[asyncio]>=2.0,<3
alembic>=1.13,<2

# ── Async / concurrency ───────────────────────────────────────────────────────
anyio>=4.0,<5
websockets>=12.0,<14

# ── Local dev ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0,<2
