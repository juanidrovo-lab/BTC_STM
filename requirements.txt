# Runtime dependencies installed by Vercel's pip step at build time.
# Keep pin ranges in sync with pyproject.toml.
#
# Note: Vercel's @vercel/python builder bundles all project files but
# does not guarantee running `pip install -e .` for local packages.
# api/index.py adds src/ to sys.path before any btc_stm import.
fastapi>=0.110,<1
anyio>=4.0,<5
python-multipart>=0.0.9,<1
httpx>=0.27,<1
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
sqlalchemy[asyncio]>=2.0,<3
asyncpg>=0.29,<1
alembic>=1.13,<2
websockets>=12.0,<14
