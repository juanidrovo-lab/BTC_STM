# Runtime dependencies for Vercel Python build.
# This file is read by Vercel's pip install step during deployment.
# Pin ranges match pyproject.toml; update both in sync.
fastapi>=0.110,<1
httpx>=0.27,<1
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
sqlalchemy[asyncio]>=2.0,<3
asyncpg>=0.29,<1
alembic>=1.13,<2
websockets>=12.0,<14
